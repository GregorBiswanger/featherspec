---
name: sdd-reverse-specify
description: Reverse specification — reconstruct existing behaviour from code, tests or a plan into a human-validated Baseline spec. Resumable.
argument-hint: "[target: path | folder | symbol | plan file] (optional)"
---

# /sdd-reverse-specify

Follow [`.claude/commands/sdd-reverse-specify.md`](../../.claude/commands/sdd-reverse-specify.md)
exactly — read that file first and treat it as your instructions for this turn. It is the
single source for this workflow; this file only points at it and holds no rules of its own.

Anything the user typed after the command is the argument the workflow refers to.
