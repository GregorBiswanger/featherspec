---
name: sdd-setup
description: Onboarding wizard — set DocLanguage, seed the Memory Bank, capture the first architecture snapshot.
argument-hint: "[docLanguage] [projectName] [stack] — or just answer the wizard"
---

# /sdd-setup

Follow [`.claude/commands/sdd-setup.md`](../../.claude/commands/sdd-setup.md) exactly — read that file
first and treat it as your instructions for this turn. It is the single source for this
workflow; this file only points at it and holds no rules of its own.

Anything the user typed after the command is the argument the workflow refers to.
